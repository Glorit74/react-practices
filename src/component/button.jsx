

const Button = ({buttonText, color}) => {
    const onClick = () => {
                
        
    }

    return (
        <button style={{backgroundColor: color}} onClick={onClick}>{buttonText}</button>
    )
}

export default Button